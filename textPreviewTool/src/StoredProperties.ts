export interface StoredProperties {
  readonly bottom_line_distance: number;
  readonly line_width: number;
  readonly spacing_x: number;
  readonly text: string;
  readonly textAreaWidth: string
  readonly textAreaHeight: string
  readonly textType: "character" | "word"
}
